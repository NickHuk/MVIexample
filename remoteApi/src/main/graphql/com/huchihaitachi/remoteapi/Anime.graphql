query Page($page: Int, $perPage: Int) {
    Page(page: $page, perPage: $perPage) {
        pageInfo {
            total
            perPage
            currentPage
            lastPage
            hasNextPage
        }
        media {
            id
            title {
                english
            }
            type
            description(asHtml: false)
            coverImage {
                large
            }
            episodes
            duration
        }
    }
}